spring:
  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://172.16.143.41:5432/auth}
    username: ${AUTH_DB_USERNAME:paymedia}
    password: ${AUTH_DB_PASSWORD:paymedia}
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: HikariCP-uat
      initialization-fail-timeout: -1
