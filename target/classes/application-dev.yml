spring:
  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://172.16.133.41:5432/auth}
    username: ${AUTH_DB_USERNAME:paymedia}
    password: ${AUTH_DB_PASSWORD:paymedia}
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: HikariCP-dev
      initialization-fail-timeout: -1
